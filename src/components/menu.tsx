import { Link } from "react-router-dom";

const Menu = () => {
    <div>
        <nav className='menu'>
            <Link to='/' className='menu-link'>PÁGINA INICIAL</Link>
        </nav>
    </div>
}

export default Menu;